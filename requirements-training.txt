# Install ROCm PyTorch FIRST (before this file):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm6.1
#
# Then:
#   pip install -r requirements-training.txt
#
# DO NOT install bitsandbytes for training — it is incompatible with ROCm 6.x.
# Standard LoRA bf16 is used instead (see train.py).

transformers>=4.40.0
peft>=0.10.0
trl>=0.8.6
datasets>=2.18.0
accelerate>=0.27.0
huggingface-hub>=0.21.0
safetensors>=0.4.0
